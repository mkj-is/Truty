#!/usr/bin/env ruby -rubygems

require "truty"

def main
  language = :general
  if ARGV[0] == "-l" || ARGV[0] == "--language" then
    language = ARGV[1]
    ARGV.shift(2)
  end
  puts Truty.send :fix, ARGF.read, language
end

main
